# TODO

未着手の作業項目。着手する際は `state/tasks/T-NNN.md` として起票する。

---

## 優先度：高

- [ ] **GitHub App でエージェント専用アイデンティティを用意する** — 現状は人間もエージェントも同一の `rikunishi45` として GitHub を操作するため、サーバー側でクラスCの自動マージを止められない（ADR-003）。App を導入すると PR作成者が `xxx[bot]` になり、`rikunishi45` がコードオーナーとして承認できるようになり、`main-review` の管理者bypassを撤去できる。秘密鍵の保管方針の決定が前提

## 優先度：中

- [ ] **organization へのリポジトリ移管を検討する** — マージキューは組織所有リポジトリ専用で、ユーザー所有では API が `merge_queue` ルールを拒否する（T-004 Step 14 のブロック理由）。移管すればマージキューが使えるが、`classify.yml` に `merge_group` トリガーを足す改修が前提。並列開発の規模が上がってから判断する

## 優先度：低

- [ ] [項目を追加]

---

## 完了

<!-- 完了した項目は日付付きでここに移動：- [x] YYYY-MM-DD — [内容] -->
