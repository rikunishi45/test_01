---
name: documentation
description: CLAUDE.md・ADR・設計書・README・ポストモーテムの作成と更新。ドキュメント成果物を書くときに使う。
model: sonnet
---

ドキュメント成果物を書く。

> このスキルが有効な間、モデルは sonnet に切り替わる（`governance/routing.md` の割り当て）。切り替わるのは現在のターンの間だけで、次のプロンプトでセッションのモデル（Opus 5）に戻る。

---

## ドキュメントの種類

### CLAUDE.md（プロジェクト用）

目的：Claude Codeにプロジェクトの完全な作業文脈を与える。

必須セクション：
1. **セッションの基本姿勢** — Supervisorとしての振る舞いと委任先
2. **プロジェクト概要** — このプロジェクトが何であり、スコープは何か
3. **ディレクトリ構成** — 主要ディレクトリとその責務
4. **技術スタック** — 言語、フレームワーク、主要ライブラリとバージョン
5. **品質基準** — 「完了」の定義（テスト、型チェック、リント）
6. **作業プロトコル** — このプロジェクトでのエージェント・スキル・メモリ・ステートの使い方

任意（該当する場合に追加）：
- 外部依存と認証要件
- 既知の落とし穴や自明でない制約
- デプロイと環境セットアップ

### アーキテクチャ決定記録（ADR）

`memory/decisions.md` に追記する。

```markdown
## ADR-NNN: [決定のタイトル]

**Date:** YYYY-MM-DD
**Status:** proposed | accepted | deprecated | superseded by ADR-NNN

### Context（背景）
[この決定に至った状況と要因]

### Decision（決定）
[何を決めたか、直接的に記述]

### Consequences（帰結）
[この結果、何が容易に・困難に・変わるか]

### Alternatives Considered（検討した代替案）
[他の選択肢と、選ばなかった理由]
```

### 設計ドキュメント

```markdown
## [機能/システム名] 設計

**Author:** [名前]
**Date:** YYYY-MM-DD
**Status:** draft | review | approved

### Goal（目的）
[何の問題を、誰のために解決するか]

### Non-Goals（対象外）
[明示的な除外事項]

### Design（設計）
[技術的な説明。必要なら図を添える]

### Implementation Plan（実装計画）
[ステップ、タイムライン、依存関係]

### Open Questions（未解決の課題）
[未解決の問題]
```

---

## 執筆基準

- **正確性：** すべての記述は「今」真実であること。願望ではなく事実。コードがまだ実装していないなら「予定」「未実装」と書く。
- **精密さ：** 曖昧な表現を避ける。「エラーを適切に処理する」ではなく「構造化されたエラーボディとともにHTTP 422を返す」と書く。
- **完全性：** 読者が追加の質問なしにドキュメントだけで行動できること。
- **簡潔さ：** 読者が既に知っている背景は省く。すべての文がそこにある理由を持つこと。

---

## メンテナンスルール

- ドキュメントは、それが説明するコード変更と同じコミットで更新する。
- 古くなったセクションには `> **Note:** このセクションはYYYY-MM-DD時点の情報。利用前に検証すること。` を付ける。
- 現実を反映しなくなったドキュメントは削除する — 古いドキュメントはドキュメントがないより悪い。

---

## 完成後

外部に出る文書（README、設計書、ADR）は `doc-verifier` エージェントに採点させてから確定する（`governance/verification.md` の発動条件を参照）。
